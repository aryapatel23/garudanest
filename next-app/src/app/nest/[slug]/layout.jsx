export const metadata = {
  title: 'Engineer Profile',
  description: 'Explore engineer profile, skills, portfolio links, and capabilities in the GarudaNest Nest.',
  alternates: {
    canonical: '/nest'
  }
};

export default function NestMemberLayout({ children }) {
  return children;
}
